<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.berhitung">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Penjumlahan"></activity>
        <activity android:name=".Pengurangan"></activity>
        <activity android:name=".Pembagian"></activity>
        <activity android:name=".Perkalian"></activity>
        <activity android:name=".UjianBerhitung"></activity>
        <activity android:name=".HasilKuis"></activity>

    </application>

</manifest>